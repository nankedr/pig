# 隔离 Pig 身份、状态与外部服务

用户身份、默认状态和自有环境变量统一使用 `pig`、`.pig`、`~/.pig` 与 `PIG_*`，正常运行不读取 Pi 活动状态或冒用 Pi 托管服务身份；只有互操作所需的既有线标识保持原值。`pig` 与 `pig-ai` 共享 canonical 默认凭证库 `~/.pig/agent/auth.json`，其他文件仅通过显式路径使用，这有意偏离 `pi-ai` 的当前目录 `auth.json`。隔离品牌、状态与服务可以保持协议兼容，又避免污染 Pi 安装或错误归因。
