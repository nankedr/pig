# 固定对等基线与按需 Oracle

Pig V1 固定复刻 Pi 提交 `936aff00918de1187f085f123c2812d8f2d67745`，并以不可变模型目录快照补足该提交未包含的聊天模型数据；升级上游只能作为完成 V1 后的独立迭代。Pi 只作为按需获取并校验的 Oracle，不是 submodule 或运行时依赖，Parity Catalog 是范围、映射、状态与证据的唯一权威。固定两件基线制品而非追踪 `main`，是为了让“1:1 完成”成为可复现、可审计的命题。
