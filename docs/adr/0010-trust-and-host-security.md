# 明确项目信任与宿主执行边界

Project Trust 是项目设置、资源、包和扩展的执行门，不是逐 Tool 审批、路径边界或 sandbox；受信内容和内建 Tool 继承宿主用户权限。Context File 即使未信任也会进入模型上下文，但 Pig 在信任结论前不读取任何 trust-sensitive 项目设置，以修复固定 Pi 快照的启动缺口。明确这些边界是为了保留 Pi 能力，同时不向用户承诺不存在的安全隔离。
